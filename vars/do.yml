digitalocean_token: "{{ lookup('ansible.builtin.env', 'DIGITALOCEAN_TOKEN') }}"
public_key: "{{ lookup('ansible.builtin.file', lookup('ansible.builtin.env', 'JUKE_SSH_PUBKEY')) }}"
env: "{{ lookup('ansible.builtin.env', 'JUKE_ENV') }}"
# TODO: figure out how to get setting this to 0 to spin down machines
desired_number_of_webservers: 2