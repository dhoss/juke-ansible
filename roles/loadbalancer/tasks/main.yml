---
- name: Install Nginx
  apt:
    name: nginx
    state: present

- name: Nginx load balancer configuration
  ansible.builtin.template:
    src: nginx.j2
    dest: /etc/nginx/nginx.conf
    owner: root
    group: root
    mode: '0644'